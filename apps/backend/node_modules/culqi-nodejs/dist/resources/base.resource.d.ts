import { HttpClient, RequestConfig } from '../client/http-client';
/**
 * Shared helpers for resource classes.
 * Handles path concatenation, query parameters and public‑key flag.
 */
export declare abstract class BaseResource {
    protected readonly http: HttpClient;
    protected readonly basePath: string;
    protected constructor(http: HttpClient, basePath: string);
    protected get<T>(path?: string, cfg?: RequestConfig): Promise<T>;
    protected post<T>(path?: string, cfg?: RequestConfig): Promise<T>;
    protected patch<T>(path?: string, cfg?: RequestConfig): Promise<T>;
    protected del<T>(path?: string, cfg?: RequestConfig): Promise<T>;
}
//# sourceMappingURL=base.resource.d.ts.map